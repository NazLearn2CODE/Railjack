---
title: Project Index
created: 2026-06-30
project: Orbiter
---

# Orbiter — Project Index

## Purpose

A locally-hosted **Agentic OS**: an orchestration layer that treats AI agents as
OS processes — with scheduling, sandboxing, and observability — exposed through a
web dashboard on `localhost:<port>`. Built from the blueprint in
`[[agentic-os-guide]]`.

## Quick Context for Claude

**Tech stack:**
- Language: **Python** (backend + OS core)
- Backend: FastAPI + `claude-agent-sdk` (v0.1.81) + WebSocket streaming
- Frontend: React 19 + Vite + Tailwind v4 + Zustand
- Core runtime: `asyncio` (scheduling primitives)
- Key deps: see `../pyproject.toml`

**Where to find what:**
- Blueprint (full): `[[agentic-os-guide]]`
- Architecture overview: `[[architecture]]`
- API reference: `[[api-reference]]`
- Project decisions: `[[decisions]]`
- Session logs: `[[../B-sessions]]`

**Current status:**
- **🏁 MILESTONE — first live run + visual verification (2026-07-02, end of session):** Orbiter ran end-to-end as a real product for the first time — not just tests. `uvicorn app.main:app --port 8000` serves the built React dashboard at `/` (FastAPI mounts `web/dist`) on `localhost:8000`; the z.ai/GLM backend drove real agents live in-browser — single-agent WS token stream, the gated-tool `ApprovalCard`, and TEAM-mode `delegate_many` fan-out all confirmed working. **User reviewed the dashboard for the first time** (≈4 days building, first look) — HUD aesthetic, composer, console, telemetry, and the honest `FAIL-OPEN` sandbox posture on the HOST panel all render correctly. This closes the "inspection-verified / DOM-verified but never eyeballed" gap: the UI is now human-verified, not just tsc-green. **Session arc:** shared team token-budget pool shipped (`7d6dc92`) → independent ponytail-audit cut the unused `team_budget_ceiling` param (`65615a6`) → backend standardized on z.ai/GLM, native Anthropic off the roadmap (`3299edf`). 61 pytest green · ruff clean · `tsc --noEmit` + `npm run build` clean. **The program runs; no increment is *needed* to run it** — next work is demand-driven (see NEXT INCREMENT).
- **`main` is the working line** — early feature branches (`feat/gateway-dashboard`, `feat/security-l3-landlock`) merged in fast-forward; later increments commit directly to `main` (head advances directly on `main`; latest this session — shared team token-budget pool `7d6dc92` + ponytail-audit cut `65615a6`; see `git log`). On main now: FastAPI gateway (REST + WS), React 19 dashboard, HiveMind scheduler primitives (AIMD / circuit-breaker / rate-limit / admission / token-budget), streaming agent loop, **per-call tool approval via a PreToolUse hook** (Bash/Write/Edit gated, read-only auto-run — `[[2026-07-01-tool-approval-pretooluse-hook]]`), a **security policy floor** (L1/L2/L4 — `[[2026-07-01-security-l1-l2-l4]]`), a **kernel write-confinement backstop** (L3 self-Landlock at startup — `[[2026-07-01-sandbox-l3-landlock]]`), the **`Provider` trait** (`[[2026-07-01-provider-protocol]]`), the **centralized 2DOT topology** (`[[2026-07-02-centralized-2dot-topology]]`) + its gateway/dashboard surface, **external MCP** (`[[2026-07-02-external-mcp]]`), and **worker streaming lanes** (`[[2026-07-02-worker-streaming-lanes]]`) with **actionable worker approvals** (`[[2026-07-02-actionable-worker-approvals]]`).
- **Browser-verified end-to-end** under the z.ai/GLM backend: composer → WS token stream → completion, and approval card → APPROVE → gated Bash executes. ADR #8's "PreToolUse hooks fire under z.ai" upgraded from a code spike to a confirmed UI loop. Token telemetry **and** the per-session budget both count full throughput (input + cache + output); budget ceiling 200k.
- **Codebase audit-clean + type-safe:** ponytail audit collapsed the last duplication/dead code; `tsc --noEmit` is green and `npm run build` gates on it.
- **Security floor shipped (blueprint §2.2 L1/L2/L4):** `app/core/security.py` enforces a policy floor at the PreToolUse hook that **outranks operator approval** — catastrophic shell commands (`rm -rf /`, `mkfs`, `dd of=/dev/…`, fork bombs, pipe-to-shell, …) and out-of-workspace `Write`/`Edit` hard-deny *before* the approval card; every gated call writes one HMAC-SHA256 receipt to `logs/receipts.jsonl`. Env-configured (`ORBITER_WORKSPACE_ROOT` / `ORBITER_RECEIPT_LOG` / `ORBITER_RECEIPT_SECRET`). 26 tests green — see `[[2026-07-01-security-l1-l2-l4]]`.
- **L3 OS sandbox shipped (blueprint §2.2 Layer 3):** `app/core/sandbox.py` self-Landlocks the Orbiter process at startup via raw syscalls (`ctypes`, no new dep) — WRITE-type accesses confined to a small allowlist (workspace root, `/tmp`, `~/.claude`, + `ORBITER_SANDBOX_EXTRA_ROOTS`); reads/exec stay open. Restrictions inherit to the claude-agent-sdk CLI + its native Bash subprocess. Fail-open if Landlock is unavailable; status reported on `GET /api/health`. ADR `[[2026-07-01-sandbox-l3-landlock]]`, plan `[[2026-07-01-l3-landlock-sandbox]]`. **Runtime caveat:** this dev box's kernel has *no active Landlock LSM* (`/sys/kernel/security/landlock` absent — the probe returns `EFAULT`, not the `EPERM` the ADR predicted; fail-open is the observed path either way, and the syscall mechanics were confirmed via a `getpid` positive control). The gated confinement self-check (`ORBITER_SANDBOX_LIVE=1`) must pass on a landlock-capable host before L3 is runtime-confirmed. 31 tests green (1 gated check skips by default).
- **Provider trait shipped (blueprint §2.1 microkernel):** `app/core/provider.py` defines the `Provider` `Protocol` — the OS core now depends on the LLM trait, not `claude-agent-sdk`. `ClaudeSdkProvider` absorbs all SDK coupling (query/options/hook/serialize); `FakeProvider` (`tests/fakes.py`) is the second impl and runs the agent loop SDK-/network-free. **Channel deferred** (single impl today — the WS gateway). ADR `[[2026-07-01-provider-protocol]]`, plan `[[2026-07-01-provider-extraction]]`. 36 tests green.
- **Centralized 2DOT topology shipped (blueprint §1.1 + §3.2):** `app/core/orchestrator.py` — `Team` (supervisor + hired `WorkerRole`s) and the `delegate(role, task)` OS primitive. A worker **is** an `AgentSession`, so every delegation flows through the shared HiveMind scheduler (admission/AIMD/circuit-breaker/rate-limit/budget) and the security approval gate + receipt ledger — workers are first-class OS processes, not SDK-internal subagents (which would bypass the core). The supervisor gets a `delegate` tool via the SDK's **in-process MCP server** (`create_sdk_mcp_server` + `@tool`; the `mcp` package is already vendored — no new deps); workers run on a plain provider with no `delegate`, so depth is capped at 1. `delegate` never raises (failure/over-budget → string the supervisor can act on). ADR `[[2026-07-02-centralized-2dot-topology]]`. 43 tests green.
- **Topology surfaced (blueprint Phase 2 → gateway/dashboard):** `POST /api/teams` (`app/main.py`) builds a `Team`, hires roles (default `researcher` + `coder`, or caller-supplied), spawns the supervisor, and registers it — so a supervised run is driven and observed through the *existing* `/ws/sessions/{id}` stream, `/approve` gate, and `GET /api/sessions/{id}` (delegation surfaces as `delegate` tool calls + results in the Console). `AgentSession.kind` ("single" | "supervisor") badges it; `AgentSessionManager.register()` adopts the externally-built supervisor. Dashboard composer gains a `○/● TEAM` mode toggle → `createTeam()`. Default supervisor system-prompt is generated from the hired roles (`default_supervisor_prompt`). **Deferred (YAGNI, w/ triggers):** an in-dashboard role editor; a dedicated multi-lane team view.
- **External MCP shipped (blueprint §3.2 host/client):** `ClaudeSdkProvider(mcp_servers=...)` merges operator-configured external servers (stdio/sse/http) into the SDK `mcp_servers` option *alongside* the in-process `orbiter` delegate server (supervisor gets both; workers/single sessions get external only). Configured globally via `ORBITER_MCP_SERVERS` (JSON `{name: spec}`) — applied to every session, surfaced as name+type on `GET /api/health` (never env/headers — may carry secrets). The `Provider` Protocol stays SDK-free (concrete-impl concern, like `delegate`). ADR `[[2026-07-02-external-mcp]]`. **Trust boundary:** external MCP tools are operator-installed → trusted, so they bypass the PreToolUse gate (which matches only Bash/Write/Edit) — same posture as the `delegate` tool.
- **Worker streaming lanes shipped (richer team observability):** a worker's inner activity now streams onto the **supervisor's** bus — `AgentSession.event_sink` forwards every worker event as a nested `worker_event` frame (`{role, worker_id, event}`) via `Team.delegate`, and `AgentSession.ingest()` persists it to the supervisor's `messages` so it streams live *and* replays on `GET /api/sessions/{id}`. The dashboard renders each delegation as an inline `worker_lane` sub-panel (◂ DELEGATED · role + status pip) right where the supervisor called `delegate`, reusing the existing `Message` renderer for the worker's text/thinking/tool-use/result. No new endpoint — the supervisor's single `/ws` stream carries everything. ADR `[[2026-07-02-worker-streaming-lanes]]`. **Honest gap:** worker `approval_needed` is forwarded but shown only as a muted `⏸ WORKER GATE` indicator — workers aren't registered with the manager, so their gates aren't independently actionable yet (the next increment). 56 tests green.
- **Actionable worker approvals shipped (closes the lanes ADR's honest gap):** a delegated worker's gated tool (the `coder` role's Write/Edit/Bash) used to block until `APPROVAL_TIMEOUT` (600s) → fail-closed, because workers weren't registered with the manager. Now first-class approvable with **no new endpoint**: `Team(register=...)` (mirrors the `delegate`-callback pattern, wired to `manager.register` in `main.py`) sets `kind="worker"` and registers each worker before `run()`; `AgentSessionManager.list_sessions()` excludes `kind == "worker"` (sidebar stays clean — workers are observed via the supervisor's lane) while the existing `POST /api/sessions/{id}/approve` + `/ws` + GET detail drive them unchanged. Frontend: the `worker_lane` renders a real **shared `ApprovalCard`** (extracted from `Console.tsx`, bound via `onResolve`) that POSTs to `/api/sessions/{workerId}/approve` via a new `approveWorker` store action — the muted `⏸ WORKER GATE` indicator is gone. `tests/fakes.py` gains `FakeGatedProvider` (drives one call through `on_tool_use`). ADR `[[2026-07-02-actionable-worker-approvals]]`. 57 tests green.
- **Dashboard host-posture panel shipped (blueprint Phase 2 — observability):** the frontend now consumes `GET /api/health` — a read-only `04 / HOST` panel in `Telemetry` (reusing the existing `Panel`/`Readout` primitives — no new component, no backend change) shows the real **sandbox posture** (`<MECHANISM>` when active, `FAIL-OPEN` muted when not — the no-Landlock caveat on this dev box surfaced honestly, not greenwashed) and the **MCP server surface** an agent will reach (`name · type` per loaded server, or `NONE LOADED`). Secrets stay server-side: `/api/health` emits name+type only (confirmed by curl — an `http` server's `url`/`headers` never reach the client). New `getHealth()` + `Health`/`SandboxStatus`/`McpServer` types; `store.init()` fetches health independently of `listSessions()`. ADR `[[2026-07-02-dashboard-host-panel]]`. Observability only — the external-MCP ADR's deferred *config* UI stays YAGNI.
- **In-dashboard role editor shipped (TEAM mode → custom rosters):** the dashboard can now compose a custom worker roster before dispatch — a team-only `+ ROLES` reveal toggle in `Composer` (mirroring the `+ SYS PROMPT` pattern) opens a panel where each role's `name` + `system_prompt` is edited/added/removed; empty-name rows are dropped on dispatch, and an empty roster keeps the server's `DEFAULT_ROLES` (researcher + coder) — server semantics (`roles` hired verbatim, else defaults) surfaced honestly. Threads the already-existing `POST /api/teams` `roles` param via `RoleSpec` (`types.ts`) → `store.dispatch(prompt, sys?, roles?)` → `createTeam`; **no backend change** (confirmed by curl: custom `[{architect},{tester}]` → hired as-is; `null` → researcher+coder). Roles live in Composer local state (persist across dispatches, like `sys`). *Ponytail:* index keys on the small role list (focus may jump on middle-removal — stable ids if the roster grows). ADR `[[2026-07-02-dashboard-role-editor]]`.
- **`delegate_many` fan-out shipped (Centralized 2DOT route 2 — concurrent delegation):** `Team.delegate_many(delegations)` **replaces** the sequential `delegate` — `asyncio.gather` over N workers, each still a first-class `AgentSession` through the shared HiveMind scheduler (admission/AIMD/circuit-breaker/rate-limit/budget) + security gate + receipt ledger. Admission already bounds concurrency (AIMD live limit, init 4 / max 10) — no extra cap (YAGNI). Results aggregate as per-role markdown sections in input order; the extracted `_run_worker` never raises, so gather can't throw (a mixed fan-out returns all N sections). The `_to_supervisor` closure tags every `worker_event` with a shared `fanout_id` (the lane grouping key). Provider swaps the `delegate` callback + in-process MCP tool for `delegate_many` (`{delegations:[{role,task}]}` — the **`Provider` Protocol stays unchanged**). Dashboard renders the N concurrent lanes as a grouped `fanout` block (`▾ FAN-OUT · N workers [X live · Y done]`, each lane reusing the existing `worker_lane` renderer + `ApprovalCard`) — the multi-lane view that was YAGNI *until* fan-out. The standalone top-level `worker_lane` path is removed (replace). ADR `[[2026-07-02-delegate-many-fanout]]`, plan `[[2026-07-02-delegate-many-fanout-plan]]`. **Integration boundary:** the OS primitive is SDK-free tested incl. a concurrency-overlap proof (`FakeBlockingProvider`: >1 worker in-flight at once; sequential dispatch deadlocks at 1); the *supervisor LLM choosing* to call `delegate_many` and filling the array-of-objects schema is the remaining real-LLM risk under z.ai/GLM. 59 tests green.
- **`delegate_many` verified end-to-end under z.ai/GLM — integration boundary closed (+ two latent fixes):** the real-LLM drive (`[[2026-07-02-delegate-many-verified]]`) confirmed (a) the supervisor emits `mcp__orbiter__delegate_many` with a well-formed `delegations:[{role,task}…]` array, (b) 2 workers run concurrently under one `fanout_id` (overlapping active windows), (c) a gated worker lane's `approval_needed` resolves via `POST /api/sessions/{workerId}/approve` → gated tool executes → HMAC receipt minted. The drive **exposed two latent bugs** that shipped green-but-broken because every test runs SDK-free via `FakeProvider` (never the real CLI): **(1)** supervisor/worker `session_id` carried a `supervisor-`/`worker-` prefix → the CLI rejects non-UUID ids at init (exit 1, before any LLM call) — now `str(uuid.uuid4())`; **(2)** the in-process MCP tool is exposed as `mcp__orbiter__delegate_many`, but `allowed_tools` carried only the bare `delegate_many` → the CLI denied the call as unpermitted — `ClaudeSdkProvider.stream()` now expands `allowed_tools` with the namespaced name (MCP-naming knowledge centralized in the provider), plus a `delegations` string→array coercion at the tool boundary (z.ai sometimes stringifies it). 59 pytest green · ruff clean (frontend untouched). Browser drive confirmed the grouped `▾ FAN-OUT` block renders with lanes + the delegate_many TOOL row (live DOM); the inline `ApprovalCard` pixel was not captured (z.ai's coder nondeterministically skipped the gated tool on browser runs) — its data flow is WS-proven and rendering is inspection-verified + tsc-green.
- **Shared team token-budget pool shipped (bounds a whole fan-out — was NEXT INCREMENT):** `delegate_many` fan-out's per-worker independent 200k ceilings are replaced by **one shared pool** — the supervisor + every worker bill against a single team key with a team-sized ceiling, so a fan-out is bounded as a whole and an under-budget worker's headroom subsidizes a costlier one. `TokenBudgetManager` (`scheduler.py`) gains per-key ceilings (`set_ceiling`/`effective_ceiling`; the previously-dead `ceiling` param on `check_budget` removed — deletion over addition); `AgentSession` (`agent.py`) gains a `budget_key` seam (`run()` resolves `budget_key or session_id` once and keys `enter_turn`/`consume`/the over-budget message on it, reporting the *real* ceiling via `effective_ceiling`); `Team` (`orchestrator.py`) mints `budget_key = "team-<hex8>"`, `supervisor()` establishes the pool (`team_budget_ceiling or default_ceiling × (1 + len(roles))`) and stamps the key on supervisor + each worker. Plain sessions untouched (`budget_key=None` → keys on `session_id`, falls through to `default_ceiling` — identical to before). Default team (researcher + coder) → 600k pool. ADR `[[2026-07-02-team-budget-pool]]`. 61 tests green (+2: a per-key-ceiling unit + a deterministic shared-pool fan-out proof — 2 workers × 60 tokens vs a 100-token pool → exactly one over budget, because `consume` is an atomic sync block). `# ponytail:` breadth proxied by *hired roles*, not fan-out cardinality; no env knob (add `ORBITER_TEAM_BUDGET` when a run needs to override).
- **🏁 MILESTONE — Mission-Control Surface shipped, reviewed, cleaned + live-verified (2026-07-06):** the brief's remaining surface delta, planned in `[[../A-project/plans/2026-07-06-mission-control-surface]]` (mirror at `~/.claude/plans/project-name-orbiter-goal-jaunty-catmull.md`). **Implemented by Gemini** (commit `3a03e89`), then **reviewed + hardened this session** (Opus). Five milestones: **M1** left tab bar (AGENTS/SKILLS/SERVICES) + services launcher tiles (`ORBITER_SERVICES` env; iframe in the Console when `embed:true`); **M2** skills scan (`app/core/skills.py` + `GET /api/skills`; active skill resolved client-side in `util.activeSkill`, shown in Telemetry `02/SESSION`); **M3** Cephalon compliance probe (`app/core/cephalon.py` → `workspace` on `/api/health`; full/partial/none + per-check pips in Telemetry `05/PROJECT`); **M4** provider registry + model switcher (`ORBITER_PROVIDERS`, secrets-redacted `/api/providers`, per-session `ClaudeSdkProvider(model=,env=)` routing — SDK `env` **merges** over inherited env, confirmed in `subprocess_cli.py`; Composer `MDL ▸` cycle selector); **M5** `A-project/extending.md`. **Review pass (`/ponytail-audit` + `/code-review`) found + fixed one real correctness bug:** `registry.resolve()` injected a provider token as `ANTHROPIC_API_KEY` while the ambient `ANTHROPIC_AUTH_TOKEN` survived the env-merge → the CLI would send the *stale* token (auth failure + token leak to the new host) for any configured 2nd provider; now writes `ANTHROPIC_AUTH_TOKEN` so the merge overrides cleanly (`test_registry.py` encodes it). **Cleanups applied:** `ProviderRegistry` class → plain module funcs + `PROVIDERS` const (matches `EXTERNAL_MCP`/`SERVICES`); shared `_load_json_env` helper for the 3 env-JSON loaders; Composer hooks lifted out of an in-JSX IIFE (rules-of-hooks fix); Telemetry's 4 copy-pasted check rows → a `.map`; lazy `registry`/`cephalon`/`skills` imports hoisted. **Live-run verified:** browser smoke green (0 console errors, WS stream → completion 18.5k tok → approval loop), and all new surfaces eyeballed — tab bar, SKILLS (78 scanned), SERVICES empty-state, and `05/PROJECT` showing this repo's real `CEPHALON ● PARTIAL` (CLAUDE.md/CodeCompass/index ✓, Obsidian MCP ✗). **74 pytest green · ruff clean · `tsc --noEmit` + `npm run build` clean.**
- **Backend standardized on z.ai/GLM — native Anthropic not planned:** z.ai/GLM is this project's chosen LLM backend (no `sk-ant-` key in env/`.env`, and none intended). Native Anthropic-API verification is therefore **off the roadmap**, not deferred-pending-a-key. The recipe at `[[2026-07-02-native-anthropic-verification-deferred]]` is retained for reference only — backend selection is env-only, so it remains a pure key swap if that choice ever reverses. *Deferred (YAGNI, w/ triggers):* per-role `allowed_tools` editing (API ships name+system_prompt only today); hierarchical/decentralized topologies; per-worker autonomy levels; **Channel** (second microkernel trait — single impl today, the WS gateway).
- Last deployed: n/a (dev only, `localhost:8000`).

## Build Rules

- **UI / dashboard work → invoke the `frontend-design` skill** (plugin installed globally).
- OS-core scheduling primitives map onto `asyncio` (semaphores, AIMD controller, circuit breaker).
- Rust microkernel is the guide's ideal but is **deferred** — see `decisions/2026-06-30-tech-stack-python.md`.

## Working conventions (read before touching code)

- **Ponytail mode is ACTIVE (full)** — shortest working diff wins; reuse over rewrite; stdlib/native first; mark deliberate shortcuts with `# ponytail:` / `// ponytail:`. Audit your own diff before every commit (correctness bugs, duplication, dead code). Understand the full flow first, *then* be lazy.
- **Verify before every commit** — all three must be green:
  - `.venv/bin/pytest -q` (backend; `tests/fakes.py:FakeProvider` runs the agent loop SDK-/network-free)
  - `cd web && npx tsc --noEmit && npm run build` (frontend; `build` gates on tsc; `dist/` is gitignored)
  - `.venv/bin/ruff check <changed files>` (lint)
- **Commit directly to `main`** (the working line), one coherent increment per commit, conventional-commit message ending with `Co-Authored-By: Claude <noreply@anthropic.com>`. Stage only the increment's files via **explicit paths** — `B-sessions/2026-07-0*-s-*.md` runtime logs are agent-run artifacts, not increments, and have been observed to land in the git **index without an explicit `git add`** (cause undetermined); always `git restore --staged B-sessions/` and confirm `git status` shows only the increment before committing. Update this index's *Current status* + *Next* as you ship.
- **OS primitives are tested SDK-free** via `FakeProvider`; the *LLM-choosing-to-call-X* boundary stays a real-LLM integration note recorded in an ADR (see `decisions/`).
- **Match the existing aesthetic** — dark HUD/phosphor (`hud--bracket`, `signal`/`hazard`/`go`/`crit` tones, `pip`, `label`, mono). No new fonts/colors.
- **Vault is READ-ONLY** (`~/Cephalon`) — never write it. Project memory lives here: `B-sessions/` (logs), `A-project/decisions/` (ADRs).

## Links to External Docs

- Blueprint source (vault, read-only): `~/Cephalon/agentic-os-guide.md`
- claude-agent-sdk: https://github.com/anthropics/claude-agent-sdk-python

---
*For Claude: this file tells you what Orbiter is and where things live. Start here, then `architecture.md`.*
