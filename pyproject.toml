[project]
name = "railjack"
version = "0.2.0"
description = "Railjack — a modular per-machine hub of local services (mission-control dashboard)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pyyaml>=6",
    "httpx>=0.27",
    "python-multipart>=0.0.18",  # FastAPI UploadFile/Form (fireside /align)
    "numpy>=2",
    "pyjwt[crypto]>=2.9",  # GA service-account JWT signing (RSA) — TRAFFIC tab
    "pillow>=10.0",  # infographic watermark crop (newsroom [inf] pipeline)
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.7"]

# Only `app` is a Python package; config/web/assets/logs are data, not code.
# Explicit so setuptools' flat-layout discovery doesn't trip on them.
[tool.setuptools]
packages = ["app"]

[tool.pytest.ini_options]
# ponytail: put project root on sys.path so `pytest` (not just `python -m pytest`)
# resolves `from app...`. Without this the bare `.venv/bin/pytest` invocation
# used in CI/plans fails with ModuleNotFoundError: No module named 'app'.
pythonpath = ["."]

[tool.ruff]
line-length = 100
